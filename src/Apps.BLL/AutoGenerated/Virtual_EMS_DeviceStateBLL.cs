//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Manual changes to this file may cause unexpected behavior in your application.
//     Manual changes to this file will be overwritten if the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections.Generic;
using System.Linq;
using Apps.Models;
using Apps.Common;
using Unity.Attributes;
using System.Transactions;
using Apps.BLL.Core;
using Apps.Locale;
using LinqToExcel;
using System.IO;
using System.Text;
using Apps.IDAL.EMS;
using Apps.Models.EMS;
using Apps.IBLL.EMS;
namespace Apps.BLL.EMS
{
	public partial class EMS_DeviceStateBLL: Virtual_EMS_DeviceStateBLL,IEMS_DeviceStateBLL
	{
        

	}
	public class Virtual_EMS_DeviceStateBLL
	{
        [Dependency]
        public IEMS_DeviceStateRepository m_Rep { get; set; }

		public virtual List<EMS_DeviceStateModel> GetList(ref GridPager pager, string queryStr)
        {

            IQueryable<EMS_DeviceState> queryData = null;
            if (!string.IsNullOrWhiteSpace(queryStr))
            {
                queryData = m_Rep.GetList(
								
								a=>a.Code.Contains(queryStr)
								|| a.Name.Contains(queryStr)
								
								);
            }
            else
            {
                queryData = m_Rep.GetList();
            }
            pager.totalRows = queryData.Count();
            //排序
            queryData = LinqHelper.SortingAndPaging(queryData, pager.sort, pager.order, pager.page, pager.rows);
            return CreateModelList(ref queryData);
        }

		public virtual List<EMS_DeviceStateModel> GetListByUserId(ref GridPager pager, string userId,string queryStr)
		{
			return new List<EMS_DeviceStateModel>();
		}
		
		public virtual List<EMS_DeviceStateModel> GetListByParentId(ref GridPager pager, string queryStr,object parentId)
        {
			return new List<EMS_DeviceStateModel>();
		}

        public virtual List<EMS_DeviceStateModel> CreateModelList(ref IQueryable<EMS_DeviceState> queryData)
        {

            List<EMS_DeviceStateModel> modelList = (from r in queryData
                                              select new EMS_DeviceStateModel
                                              {
													Id = r.Id,
													Code = r.Code,
													Name = r.Name,
													CreateTime = r.CreateTime,
          
                                              }).ToList();

            return modelList;
        }

        public virtual bool Create(ref ValidationErrors errors, EMS_DeviceStateModel model)
        {
            try
            {
                EMS_DeviceState entity = m_Rep.GetById(model.Id);
                if (entity != null)
                {
                    errors.Add(Resource.PrimaryRepeat);
                    return false;
                }
                entity = new EMS_DeviceState();
               				entity.Id = model.Id;
				entity.Code = model.Code;
				entity.Name = model.Name;
				entity.CreateTime = model.CreateTime;
  

                if (m_Rep.Create(entity))
                {
                    return true;
                }
                else
                {
                    errors.Add(Resource.InsertFail);
                    return false;
                }
            }
            catch (Exception ex)
            {
                errors.Add(ex.Message);
                ExceptionHander.WriteException(ex);
                return false;
            }
        }



         public virtual bool Delete(ref ValidationErrors errors, object id)
        {
            try
            {
                if (m_Rep.Delete(id) == 1)
                {
                    return true;
                }
                else
                {
                    return false;
                }
            }
            catch (Exception ex)
            {
                errors.Add(ex.Message);
                ExceptionHander.WriteException(ex);
                return false;
            }
        }

        public virtual bool Delete(ref ValidationErrors errors, object[] deleteCollection)
        {
            try
            {
                if (deleteCollection != null)
                {
                    using (TransactionScope transactionScope = new TransactionScope())
                    {
                        if (m_Rep.Delete(deleteCollection) == deleteCollection.Length)
                        {
                            transactionScope.Complete();
                            return true;
                        }
                        else
                        {
                            Transaction.Current.Rollback();
                            return false;
                        }
                    }
                }
                return false;
            }
            catch (Exception ex)
            {
                errors.Add(ex.Message);
                ExceptionHander.WriteException(ex);
                return false;
            }
        }

		
       

        public virtual bool Edit(ref ValidationErrors errors, EMS_DeviceStateModel model)
        {
            try
            {
                EMS_DeviceState entity = m_Rep.GetById(model.Id);
                if (entity == null)
                {
                    errors.Add(Resource.Disable);
                    return false;
                }
                              				entity.Id = model.Id;
				entity.Code = model.Code;
				entity.Name = model.Name;
				entity.CreateTime = model.CreateTime;
 


                if (m_Rep.Edit(entity))
                {
                    return true;
                }
                else
                {
                    errors.Add(Resource.NoDataChange);
                    return false;
                }

            }
            catch (Exception ex)
            {
                errors.Add(ex.Message);
                ExceptionHander.WriteException(ex);
                return false;
            }
        }

      

        public virtual EMS_DeviceStateModel GetById(object id)
        {
            if (IsExists(id))
            {
                EMS_DeviceState entity = m_Rep.GetById(id);
                EMS_DeviceStateModel model = new EMS_DeviceStateModel();
                              				model.Id = entity.Id;
				model.Code = entity.Code;
				model.Name = entity.Name;
				model.CreateTime = entity.CreateTime;
 
                return model;
            }
            else
            {
                return null;
            }
        }


		 /// <summary>
        /// 校验Excel数据,这个方法一般用于重写校验逻辑
        /// </summary>
        public virtual bool CheckImportData(string fileName, List<EMS_DeviceStateModel> list,ref ValidationErrors errors )
        {
          
            var targetFile = new FileInfo(fileName);

            if (!targetFile.Exists)
            {

                errors.Add("导入的数据文件不存在");
                return false;
            }

            var excelFile = new ExcelQueryFactory(fileName);

            //对应列头
			 				 excelFile.AddMapping<EMS_DeviceStateModel>(x => x.Code, "Code");
				 excelFile.AddMapping<EMS_DeviceStateModel>(x => x.Name, "Name");
				 excelFile.AddMapping<EMS_DeviceStateModel>(x => x.CreateTime, "CreateTime");
 
            //SheetName
            var excelContent = excelFile.Worksheet<EMS_DeviceStateModel>(0);
            int rowIndex = 1;
            //检查数据正确性
            foreach (var row in excelContent)
            {
                var errorMessage = new StringBuilder();
                var entity = new EMS_DeviceStateModel();
						 				  entity.Id = row.Id;
				  entity.Code = row.Code;
				  entity.Name = row.Name;
				  entity.CreateTime = row.CreateTime;
 
                //=============================================================================
                if (errorMessage.Length > 0)
                {
                    errors.Add(string.Format(
                        "第 {0} 列发现错误：{1}{2}",
                        rowIndex,
                        errorMessage,
                        "<br/>"));
                }
                list.Add(entity);
                rowIndex += 1;
            }
            if (errors.Count > 0)
            {
                return false;
            }
            return true;
        }

        /// <summary>
        /// 保存数据
        /// </summary>
        public virtual void SaveImportData(IEnumerable<EMS_DeviceStateModel> list)
        {
            try
            {
                using (DBContainer db = new DBContainer())
                {
                    foreach (var model in list)
                    {
                        EMS_DeviceState entity = new EMS_DeviceState();
                       						entity.Id = 0;
						entity.Code = model.Code;
						entity.Name = model.Name;
						entity.CreateTime = ResultHelper.NowTime;
 
                        db.EMS_DeviceState.Add(entity);
                    }
                    db.SaveChanges();
                }
            }
            catch(Exception ex)
            {
                throw;
            }
        }
		public virtual bool Check(ref ValidationErrors errors, object id,int flag)
        {
			return true;
		}

        public virtual bool IsExists(object id)
        {
            return m_Rep.IsExist(id);
        }
		
		public void Dispose()
        { 
            
        }

	}
}
